name: player_core
description: "Backend-agnostic abstraction layer for a Flutter audio player wrapper. Pair with audio_player for the media_kit-based implementation."
version: 0.8.4
homepage:

publish_to: none

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  equatable: ^2.0.8
  meta: ^1.17.0
  rxdart: ^0.28.0

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.5
  test: ^1.25.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
